en:
  games:
    default_row_columns: 'must have between 10 and 20'
    total_mines: "should have between %{min} and %{max}"
  moves:
    out_of_range: "should be between %{min} and %{max}"
    already_played: 'already played'
    game_over: 'is not valid because game is over'
